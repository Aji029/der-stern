# Rechnungsabgleich's OWN Supabase project — the new, empty one.
# Everything this app writes lives here.
VITE_SUPABASE_URL=https://your-rechnungsabgleich-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-rechnungsabgleich-anon-key

# der Stern's project — READ ONLY, and never signed in.
# The app reads products, orders and order_items anonymously; der Stern's own
# RLS refuses every write from an anonymous caller, so this cannot modify the
# live shop. Do not point this at the same project as above.
VITE_STERN_SUPABASE_URL=https://your-der-stern-project.supabase.co
VITE_STERN_SUPABASE_ANON_KEY=der-stern-anon-key
